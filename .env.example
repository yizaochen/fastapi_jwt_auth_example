# Database Configuration
SQLITE_DB_PATH=db.sqlite3

# JWT Secrets - CHANGE THESE IN PRODUCTION!
# Generate strong random secrets using: python -c "import secrets; print(secrets.token_urlsafe(32))"
ACCESS_TOKEN_SECRET=your_access_token_secret_here_change_in_production
REFRESH_TOKEN_SECRET=your_refresh_token_secret_here_change_in_production

# Optional: Enable SQLAlchemy SQL logging for debugging
# DB_ECHO=true
